#reqmod : "std-ext"
#reqmod : "std-ins"
#reqmod : "std-control"

#scope : std
	#func : copyString : addr.DEST : addr.SRC
		do
			drf : $td : @SRC
			mov.u8 : @DEST : @$td
			inc.addr : DEST
			inc.addr : SRC
		do_while.u8 : @$td
		return
	#end / func copyString
#end / scope std